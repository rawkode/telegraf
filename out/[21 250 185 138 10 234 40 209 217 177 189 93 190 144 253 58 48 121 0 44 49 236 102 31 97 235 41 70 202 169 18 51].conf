
[[inputs.ping]]
  ## List of urls to ping
  urls = [
    "$PING_TARGET_URL"
    ]

# Print all metrics that pass through this filter.
[[processors.printer]]

# Send metrics to nowhere at all
[[outputs.discard]]
